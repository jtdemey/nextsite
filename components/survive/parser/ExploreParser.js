const aliases = {
  attack: ['attack', 'fight', 'hit', 'assault', 'bonk'],
  go: ['go', 'move', 'walk', 'travel', 'cross', 'migrate', 'proceed', 'progress', 'relocate'],
};

export const exploreParse = raw => {
  const input = raw.trim().split(' ');
  switch(input[0]) {

  }
};